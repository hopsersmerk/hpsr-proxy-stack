; Configuración de stunnel para proxy HTTP (Squid) sobre TLS
; El certificado será proporcionado por Let's Encrypt

foreground = yes
debug = 4
syslog = no
output = /dev/stdout

; Opciones de seguridad TLS
; OpenSSL 3.x deshabilita SSLv2, SSLv3, TLSv1, y TLSv1.1 por defecto
; Forzamos TLS 1.2+ para máxima compatibilidad y seguridad
sslVersion = TLSv1.2
ciphers = HIGH:!aNULL:!MD5

[squid-https]
accept = 0.0.0.0:443
connect = squid:3128

; El certificado se montará desde certbot
; {DOMAIN} será reemplazado por setup-ssl.sh con tu dominio real
cert = /etc/letsencrypt/live/{DOMAIN}/fullchain.pem
key = /etc/letsencrypt/live/{DOMAIN}/privkey.pem

; Verificación de cliente (opcional, deshabilitado por defecto)
verify = 0
